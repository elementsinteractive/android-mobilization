import Mobilisation.Artifacts.loggingApi
import org.gradle.api.Project

object Mobilisation {
    const val githubUrl = "https://maven.pkg.github.com/elementsinteractive/android-mobilisation"
    const val version = "0.1.0"
    const val groupId = "nl.elements.mobilisation"

    object Artifacts {
        const val loggingApi = "logging-api"
    }
}
